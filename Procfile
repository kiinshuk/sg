web: python manage.py migrate && python -c "import django, os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','snapgram.settings'); django.setup(); from django.contrib.auth.models import User; from core.models import Profile; u=User.objects.filter(username='admin').first(); u and (setattr(u,'is_superuser',True) or setattr(u,'is_staff',True) or u.set_password('Admin1234!') or u.save()) or User.objects.create_superuser('admin','admin@snapgram.com','Admin1234!'); print('ADMIN READY')" && python manage.py collectstatic --noinput && gunicorn snapgram.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120
